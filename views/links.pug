doctype html 
html(lang='de')
    head 
        link(rel='stylesheet', href='css/links.css')
        title= `${name}\`s Profile`
    body 
        .container
            h1.title Peter
            .link__container
                each link in links
                    a.link(href=`${link.link}`)
                        p= link.title  